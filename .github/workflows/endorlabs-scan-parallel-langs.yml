# ─────────────────────────────────────────────────────────────────
# Generated by Saber v0.1.0 on 2026-06-05 for kotlin
#
#   Languages:    java, kotlin, javascript/typescript, swift, c/c++
#   Build:        gradle
#   Namespace:    endor-solutions-ypulse.github-ci.saber-e2e
#   Auth:         GitHub OIDC (keyless)
#   PR behavior:  comment on PR (non-blocking)
#   PR scan mode: quick
#
# Regenerate this file with `saber scan repo` (point at this repo's path).
# Hand-edits will be overwritten on the next regeneration.
# ─────────────────────────────────────────────────────────────────
#
# Saber split one or more scan units to fit standard runner memory:
#   • java: 8 shards (libraries, compiler, plugins, kotlin-native, prepare, native, analysis, other)
#     Saber split the java scan into multiple jobs because this repo has 1453
#     java root manifests — a single full-reachability scan exceeds the
#     ubuntu-latest runner memory budget. Each shard scans one top-level
#     subtree with full reachability preserved.
#   • javascript/typescript: 3 shards (libraries, js, wasm)
#     Saber split the javascript/typescript scan into multiple jobs because
#     this repo has 12 javascript/typescript root manifests — a single
#     full-reachability scan exceeds the ubuntu-latest runner memory budget.
#     Each shard scans one top-level subtree with full reachability preserved.
#
name: Endor Labs Scan
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
jobs:
  detect-changes:
    name: Detect changed paths
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    permissions:
      pull-requests: read
    outputs:
      java-libraries: ${{ steps.filter.outputs.java-libraries }}
      java-compiler: ${{ steps.filter.outputs.java-compiler }}
      java-plugins: ${{ steps.filter.outputs.java-plugins }}
      java-kotlin-native: ${{ steps.filter.outputs.java-kotlin-native }}
      java-prepare: ${{ steps.filter.outputs.java-prepare }}
      java-native: ${{ steps.filter.outputs.java-native }}
      java-analysis: ${{ steps.filter.outputs.java-analysis }}
      java-other: ${{ steps.filter.outputs.java-other }}
      kotlin: ${{ steps.filter.outputs.kotlin }}
      javascript-typescript-libraries: ${{ steps.filter.outputs.javascript-typescript-libraries }}
      javascript-typescript-js: ${{ steps.filter.outputs.javascript-typescript-js }}
      javascript-typescript-wasm: ${{ steps.filter.outputs.javascript-typescript-wasm }}
      swift: ${{ steps.filter.outputs.swift }}
    steps:
      - uses: actions/checkout@v6
      - uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |-
            java-libraries:
              - libraries/**/'**/pom.xml'
              - libraries/**/'**/build.gradle'
              - libraries/**/'**/build.gradle.kts'
              - libraries/**/'**/*.java'
            java-compiler:
              - compiler/**/'**/pom.xml'
              - compiler/**/'**/build.gradle'
              - compiler/**/'**/build.gradle.kts'
              - compiler/**/'**/*.java'
            java-plugins:
              - plugins/**/'**/pom.xml'
              - plugins/**/'**/build.gradle'
              - plugins/**/'**/build.gradle.kts'
              - plugins/**/'**/*.java'
            java-kotlin-native:
              - kotlin-native/**/'**/pom.xml'
              - kotlin-native/**/'**/build.gradle'
              - kotlin-native/**/'**/build.gradle.kts'
              - kotlin-native/**/'**/*.java'
            java-prepare:
              - prepare/**/'**/pom.xml'
              - prepare/**/'**/build.gradle'
              - prepare/**/'**/build.gradle.kts'
              - prepare/**/'**/*.java'
            java-native:
              - native/**/'**/pom.xml'
              - native/**/'**/build.gradle'
              - native/**/'**/build.gradle.kts'
              - native/**/'**/*.java'
            java-analysis:
              - analysis/**/'**/pom.xml'
              - analysis/**/'**/build.gradle'
              - analysis/**/'**/build.gradle.kts'
              - analysis/**/'**/*.java'
            java-other:
              - '**/pom.xml'
              - '**/build.gradle'
              - '**/build.gradle.kts'
              - '**/*.java'
            kotlin:
              - '**/pom.xml'
              - '**/build.gradle'
              - '**/build.gradle.kts'
              - '**/*.java'
              - '**/*.kt'
            javascript-typescript-libraries:
              - libraries/**/'**/package.json'
              - libraries/**/'**/package-lock.json'
              - libraries/**/'**/yarn.lock'
              - libraries/**/'**/pnpm-lock.yaml'
              - libraries/**/'**/rush.json'
              - libraries/**/'**/*.js'
              - libraries/**/'**/*.ts'
              - libraries/**/'**/*.tsx'
            javascript-typescript-js:
              - js/**/'**/package.json'
              - js/**/'**/package-lock.json'
              - js/**/'**/yarn.lock'
              - js/**/'**/pnpm-lock.yaml'
              - js/**/'**/rush.json'
              - js/**/'**/*.js'
              - js/**/'**/*.ts'
              - js/**/'**/*.tsx'
            javascript-typescript-wasm:
              - wasm/**/'**/package.json'
              - wasm/**/'**/package-lock.json'
              - wasm/**/'**/yarn.lock'
              - wasm/**/'**/pnpm-lock.yaml'
              - wasm/**/'**/rush.json'
              - wasm/**/'**/*.js'
              - wasm/**/'**/*.ts'
              - wasm/**/'**/*.tsx'
            swift:
              - '**/Package.swift'
              - '**/Package@swift-*.swift'
              - '**/Podfile'
              - '**/*.swift'
  scan-java-libraries-pr:
    name: "PR Scan: java (libraries)"
    needs: detect-changes
    if: github.event_name == 'pull_request' && needs.detect-changes.outputs.java-libraries == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      id-token: write
      security-events: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
      - run: ./gradlew build -x test
      - uses: endorlabs/github-action@b8992820cc4d9c9e7ded5022adf6cabe2dc11946
        with:
          namespace: "endor-solutions-ypulse.github-ci.saber-e2e"
          scan_dependencies: true
          pr: true
          enable_pr_comments: true
          github_token: ${{ secrets.GITHUB_TOKEN }}
          enable_github_action_token: true
          additional_args: "--languages=java --include-path=libraries/** --exclude-path=compiler/android-tests/** --exclude-path=repo/artifacts-tests/** --exclude-path=repo/codebase-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-compiler-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-native-compiler-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-forward-compatibility-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-tests/** --exclude-path=compiler/daemon/daemon-tests/** --exclude-path=compiler/fir/analysis-tests/** --exclude-path=compiler/fir/modularized-tests/** --quick-scan"
  scan-java-libraries-monitor:
    name: "Monitor Scan: java (libraries)"
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      id-token: write
      security-events: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
      - run: ./gradlew build -x test
      - uses: endorlabs/github-action@b8992820cc4d9c9e7ded5022adf6cabe2dc11946
        with:
          namespace: "endor-solutions-ypulse.github-ci.saber-e2e"
          scan_dependencies: true
          pr: false
          enable_github_action_token: true
          additional_args: "--languages=java --include-path=libraries/** --exclude-path=compiler/android-tests/** --exclude-path=repo/artifacts-tests/** --exclude-path=repo/codebase-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-compiler-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-native-compiler-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-forward-compatibility-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-tests/** --exclude-path=compiler/daemon/daemon-tests/** --exclude-path=compiler/fir/analysis-tests/** --exclude-path=compiler/fir/modularized-tests/**"
          sarif_file: findings.sarif
      - uses: github/codeql-action/upload-sarif@v4
        if: always()
        continue-on-error: true
        with:
          sarif_file: findings.sarif
          category: endorlabs-java-libraries
    env:
      # Auto-fallback safety net — if the local build fails on a runner, endorctl uses the platform's pre-computed reachability instead of failing the job. Full deep reachability is still the primary path on success.
      ENDOR_SCAN_ENABLE_PRECOMPUTED_CALLGRAPHS: "true"
  scan-java-compiler-pr:
    name: "PR Scan: java (compiler)"
    needs: detect-changes
    if: github.event_name == 'pull_request' && needs.detect-changes.outputs.java-compiler == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      id-token: write
      security-events: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
      - run: ./gradlew build -x test
      - uses: endorlabs/github-action@b8992820cc4d9c9e7ded5022adf6cabe2dc11946
        with:
          namespace: "endor-solutions-ypulse.github-ci.saber-e2e"
          scan_dependencies: true
          pr: true
          enable_pr_comments: true
          github_token: ${{ secrets.GITHUB_TOKEN }}
          enable_github_action_token: true
          additional_args: "--languages=java --include-path=compiler/** --exclude-path=compiler/android-tests/** --exclude-path=repo/artifacts-tests/** --exclude-path=repo/codebase-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-compiler-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-native-compiler-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-forward-compatibility-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-tests/** --exclude-path=compiler/daemon/daemon-tests/** --exclude-path=compiler/fir/analysis-tests/** --exclude-path=compiler/fir/modularized-tests/** --quick-scan"
  scan-java-compiler-monitor:
    name: "Monitor Scan: java (compiler)"
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      id-token: write
      security-events: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
      - run: ./gradlew build -x test
      - uses: endorlabs/github-action@b8992820cc4d9c9e7ded5022adf6cabe2dc11946
        with:
          namespace: "endor-solutions-ypulse.github-ci.saber-e2e"
          scan_dependencies: true
          pr: false
          enable_github_action_token: true
          additional_args: "--languages=java --include-path=compiler/** --exclude-path=compiler/android-tests/** --exclude-path=repo/artifacts-tests/** --exclude-path=repo/codebase-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-compiler-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-native-compiler-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-forward-compatibility-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-tests/** --exclude-path=compiler/daemon/daemon-tests/** --exclude-path=compiler/fir/analysis-tests/** --exclude-path=compiler/fir/modularized-tests/**"
          sarif_file: findings.sarif
      - uses: github/codeql-action/upload-sarif@v4
        if: always()
        continue-on-error: true
        with:
          sarif_file: findings.sarif
          category: endorlabs-java-compiler
    env:
      # Auto-fallback safety net — if the local build fails on a runner, endorctl uses the platform's pre-computed reachability instead of failing the job. Full deep reachability is still the primary path on success.
      ENDOR_SCAN_ENABLE_PRECOMPUTED_CALLGRAPHS: "true"
  scan-java-plugins-pr:
    name: "PR Scan: java (plugins)"
    needs: detect-changes
    if: github.event_name == 'pull_request' && needs.detect-changes.outputs.java-plugins == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      id-token: write
      security-events: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
      - run: ./gradlew build -x test
      - uses: endorlabs/github-action@b8992820cc4d9c9e7ded5022adf6cabe2dc11946
        with:
          namespace: "endor-solutions-ypulse.github-ci.saber-e2e"
          scan_dependencies: true
          pr: true
          enable_pr_comments: true
          github_token: ${{ secrets.GITHUB_TOKEN }}
          enable_github_action_token: true
          additional_args: "--languages=java --include-path=plugins/** --exclude-path=compiler/android-tests/** --exclude-path=repo/artifacts-tests/** --exclude-path=repo/codebase-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-compiler-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-native-compiler-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-forward-compatibility-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-tests/** --exclude-path=compiler/daemon/daemon-tests/** --exclude-path=compiler/fir/analysis-tests/** --exclude-path=compiler/fir/modularized-tests/** --quick-scan"
  scan-java-plugins-monitor:
    name: "Monitor Scan: java (plugins)"
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      id-token: write
      security-events: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
      - run: ./gradlew build -x test
      - uses: endorlabs/github-action@b8992820cc4d9c9e7ded5022adf6cabe2dc11946
        with:
          namespace: "endor-solutions-ypulse.github-ci.saber-e2e"
          scan_dependencies: true
          pr: false
          enable_github_action_token: true
          additional_args: "--languages=java --include-path=plugins/** --exclude-path=compiler/android-tests/** --exclude-path=repo/artifacts-tests/** --exclude-path=repo/codebase-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-compiler-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-native-compiler-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-forward-compatibility-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-tests/** --exclude-path=compiler/daemon/daemon-tests/** --exclude-path=compiler/fir/analysis-tests/** --exclude-path=compiler/fir/modularized-tests/**"
          sarif_file: findings.sarif
      - uses: github/codeql-action/upload-sarif@v4
        if: always()
        continue-on-error: true
        with:
          sarif_file: findings.sarif
          category: endorlabs-java-plugins
    env:
      # Auto-fallback safety net — if the local build fails on a runner, endorctl uses the platform's pre-computed reachability instead of failing the job. Full deep reachability is still the primary path on success.
      ENDOR_SCAN_ENABLE_PRECOMPUTED_CALLGRAPHS: "true"
  scan-java-kotlin-native-pr:
    name: "PR Scan: java (kotlin-native)"
    needs: detect-changes
    if: github.event_name == 'pull_request' && needs.detect-changes.outputs.java-kotlin-native == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      id-token: write
      security-events: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
      - run: ./gradlew build -x test
      - uses: endorlabs/github-action@b8992820cc4d9c9e7ded5022adf6cabe2dc11946
        with:
          namespace: "endor-solutions-ypulse.github-ci.saber-e2e"
          scan_dependencies: true
          pr: true
          enable_pr_comments: true
          github_token: ${{ secrets.GITHUB_TOKEN }}
          enable_github_action_token: true
          additional_args: "--languages=java --include-path=kotlin-native/** --exclude-path=compiler/android-tests/** --exclude-path=repo/artifacts-tests/** --exclude-path=repo/codebase-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-compiler-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-native-compiler-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-forward-compatibility-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-tests/** --exclude-path=compiler/daemon/daemon-tests/** --exclude-path=compiler/fir/analysis-tests/** --exclude-path=compiler/fir/modularized-tests/** --quick-scan"
  scan-java-kotlin-native-monitor:
    name: "Monitor Scan: java (kotlin-native)"
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      id-token: write
      security-events: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
      - run: ./gradlew build -x test
      - uses: endorlabs/github-action@b8992820cc4d9c9e7ded5022adf6cabe2dc11946
        with:
          namespace: "endor-solutions-ypulse.github-ci.saber-e2e"
          scan_dependencies: true
          pr: false
          enable_github_action_token: true
          additional_args: "--languages=java --include-path=kotlin-native/** --exclude-path=compiler/android-tests/** --exclude-path=repo/artifacts-tests/** --exclude-path=repo/codebase-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-compiler-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-native-compiler-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-forward-compatibility-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-tests/** --exclude-path=compiler/daemon/daemon-tests/** --exclude-path=compiler/fir/analysis-tests/** --exclude-path=compiler/fir/modularized-tests/**"
          sarif_file: findings.sarif
      - uses: github/codeql-action/upload-sarif@v4
        if: always()
        continue-on-error: true
        with:
          sarif_file: findings.sarif
          category: endorlabs-java-kotlin-native
    env:
      # Auto-fallback safety net — if the local build fails on a runner, endorctl uses the platform's pre-computed reachability instead of failing the job. Full deep reachability is still the primary path on success.
      ENDOR_SCAN_ENABLE_PRECOMPUTED_CALLGRAPHS: "true"
  scan-java-prepare-pr:
    name: "PR Scan: java (prepare)"
    needs: detect-changes
    if: github.event_name == 'pull_request' && needs.detect-changes.outputs.java-prepare == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      id-token: write
      security-events: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
      - run: ./gradlew build -x test
      - uses: endorlabs/github-action@b8992820cc4d9c9e7ded5022adf6cabe2dc11946
        with:
          namespace: "endor-solutions-ypulse.github-ci.saber-e2e"
          scan_dependencies: true
          pr: true
          enable_pr_comments: true
          github_token: ${{ secrets.GITHUB_TOKEN }}
          enable_github_action_token: true
          additional_args: "--languages=java --include-path=prepare/** --exclude-path=compiler/android-tests/** --exclude-path=repo/artifacts-tests/** --exclude-path=repo/codebase-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-compiler-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-native-compiler-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-forward-compatibility-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-tests/** --exclude-path=compiler/daemon/daemon-tests/** --exclude-path=compiler/fir/analysis-tests/** --exclude-path=compiler/fir/modularized-tests/** --quick-scan"
  scan-java-prepare-monitor:
    name: "Monitor Scan: java (prepare)"
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      id-token: write
      security-events: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
      - run: ./gradlew build -x test
      - uses: endorlabs/github-action@b8992820cc4d9c9e7ded5022adf6cabe2dc11946
        with:
          namespace: "endor-solutions-ypulse.github-ci.saber-e2e"
          scan_dependencies: true
          pr: false
          enable_github_action_token: true
          additional_args: "--languages=java --include-path=prepare/** --exclude-path=compiler/android-tests/** --exclude-path=repo/artifacts-tests/** --exclude-path=repo/codebase-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-compiler-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-native-compiler-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-forward-compatibility-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-tests/** --exclude-path=compiler/daemon/daemon-tests/** --exclude-path=compiler/fir/analysis-tests/** --exclude-path=compiler/fir/modularized-tests/**"
          sarif_file: findings.sarif
      - uses: github/codeql-action/upload-sarif@v4
        if: always()
        continue-on-error: true
        with:
          sarif_file: findings.sarif
          category: endorlabs-java-prepare
    env:
      # Auto-fallback safety net — if the local build fails on a runner, endorctl uses the platform's pre-computed reachability instead of failing the job. Full deep reachability is still the primary path on success.
      ENDOR_SCAN_ENABLE_PRECOMPUTED_CALLGRAPHS: "true"
  scan-java-native-pr:
    name: "PR Scan: java (native)"
    needs: detect-changes
    if: github.event_name == 'pull_request' && needs.detect-changes.outputs.java-native == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      id-token: write
      security-events: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
      - run: ./gradlew build -x test
      - uses: endorlabs/github-action@b8992820cc4d9c9e7ded5022adf6cabe2dc11946
        with:
          namespace: "endor-solutions-ypulse.github-ci.saber-e2e"
          scan_dependencies: true
          pr: true
          enable_pr_comments: true
          github_token: ${{ secrets.GITHUB_TOKEN }}
          enable_github_action_token: true
          additional_args: "--languages=java --include-path=native/** --exclude-path=compiler/android-tests/** --exclude-path=repo/artifacts-tests/** --exclude-path=repo/codebase-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-compiler-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-native-compiler-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-forward-compatibility-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-tests/** --exclude-path=compiler/daemon/daemon-tests/** --exclude-path=compiler/fir/analysis-tests/** --exclude-path=compiler/fir/modularized-tests/** --quick-scan"
  scan-java-native-monitor:
    name: "Monitor Scan: java (native)"
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      id-token: write
      security-events: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
      - run: ./gradlew build -x test
      - uses: endorlabs/github-action@b8992820cc4d9c9e7ded5022adf6cabe2dc11946
        with:
          namespace: "endor-solutions-ypulse.github-ci.saber-e2e"
          scan_dependencies: true
          pr: false
          enable_github_action_token: true
          additional_args: "--languages=java --include-path=native/** --exclude-path=compiler/android-tests/** --exclude-path=repo/artifacts-tests/** --exclude-path=repo/codebase-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-compiler-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-native-compiler-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-forward-compatibility-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-tests/** --exclude-path=compiler/daemon/daemon-tests/** --exclude-path=compiler/fir/analysis-tests/** --exclude-path=compiler/fir/modularized-tests/**"
          sarif_file: findings.sarif
      - uses: github/codeql-action/upload-sarif@v4
        if: always()
        continue-on-error: true
        with:
          sarif_file: findings.sarif
          category: endorlabs-java-native
    env:
      # Auto-fallback safety net — if the local build fails on a runner, endorctl uses the platform's pre-computed reachability instead of failing the job. Full deep reachability is still the primary path on success.
      ENDOR_SCAN_ENABLE_PRECOMPUTED_CALLGRAPHS: "true"
  scan-java-analysis-pr:
    name: "PR Scan: java (analysis)"
    needs: detect-changes
    if: github.event_name == 'pull_request' && needs.detect-changes.outputs.java-analysis == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      id-token: write
      security-events: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
      - run: ./gradlew build -x test
      - uses: endorlabs/github-action@b8992820cc4d9c9e7ded5022adf6cabe2dc11946
        with:
          namespace: "endor-solutions-ypulse.github-ci.saber-e2e"
          scan_dependencies: true
          pr: true
          enable_pr_comments: true
          github_token: ${{ secrets.GITHUB_TOKEN }}
          enable_github_action_token: true
          additional_args: "--languages=java --include-path=analysis/** --exclude-path=compiler/android-tests/** --exclude-path=repo/artifacts-tests/** --exclude-path=repo/codebase-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-compiler-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-native-compiler-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-forward-compatibility-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-tests/** --exclude-path=compiler/daemon/daemon-tests/** --exclude-path=compiler/fir/analysis-tests/** --exclude-path=compiler/fir/modularized-tests/** --quick-scan"
  scan-java-analysis-monitor:
    name: "Monitor Scan: java (analysis)"
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      id-token: write
      security-events: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
      - run: ./gradlew build -x test
      - uses: endorlabs/github-action@b8992820cc4d9c9e7ded5022adf6cabe2dc11946
        with:
          namespace: "endor-solutions-ypulse.github-ci.saber-e2e"
          scan_dependencies: true
          pr: false
          enable_github_action_token: true
          additional_args: "--languages=java --include-path=analysis/** --exclude-path=compiler/android-tests/** --exclude-path=repo/artifacts-tests/** --exclude-path=repo/codebase-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-compiler-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-native-compiler-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-forward-compatibility-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-tests/** --exclude-path=compiler/daemon/daemon-tests/** --exclude-path=compiler/fir/analysis-tests/** --exclude-path=compiler/fir/modularized-tests/**"
          sarif_file: findings.sarif
      - uses: github/codeql-action/upload-sarif@v4
        if: always()
        continue-on-error: true
        with:
          sarif_file: findings.sarif
          category: endorlabs-java-analysis
    env:
      # Auto-fallback safety net — if the local build fails on a runner, endorctl uses the platform's pre-computed reachability instead of failing the job. Full deep reachability is still the primary path on success.
      ENDOR_SCAN_ENABLE_PRECOMPUTED_CALLGRAPHS: "true"
  scan-java-other-pr:
    name: "PR Scan: java (other)"
    needs: detect-changes
    if: github.event_name == 'pull_request' && needs.detect-changes.outputs.java-other == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      id-token: write
      security-events: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
      - run: ./gradlew build -x test
      - uses: endorlabs/github-action@b8992820cc4d9c9e7ded5022adf6cabe2dc11946
        with:
          namespace: "endor-solutions-ypulse.github-ci.saber-e2e"
          scan_dependencies: true
          pr: true
          enable_pr_comments: true
          github_token: ${{ secrets.GITHUB_TOKEN }}
          enable_github_action_token: true
          additional_args: "--languages=java --include-path=repo/** --include-path=dependencies/** --include-path=core/** --include-path=js/** --include-path=wasm/** --include-path=generators/** --include-path=jps/** --include-path=benchmarks/** --include-path=build-common/** --include-path=test-instrumenter/** --exclude-path=compiler/android-tests/** --exclude-path=repo/artifacts-tests/** --exclude-path=repo/codebase-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-compiler-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-native-compiler-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-forward-compatibility-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-tests/** --exclude-path=compiler/daemon/daemon-tests/** --exclude-path=compiler/fir/analysis-tests/** --exclude-path=compiler/fir/modularized-tests/** --quick-scan"
  scan-java-other-monitor:
    name: "Monitor Scan: java (other)"
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      id-token: write
      security-events: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
      - run: ./gradlew build -x test
      - uses: endorlabs/github-action@b8992820cc4d9c9e7ded5022adf6cabe2dc11946
        with:
          namespace: "endor-solutions-ypulse.github-ci.saber-e2e"
          scan_dependencies: true
          pr: false
          enable_github_action_token: true
          additional_args: "--languages=java --include-path=repo/** --include-path=dependencies/** --include-path=core/** --include-path=js/** --include-path=wasm/** --include-path=generators/** --include-path=jps/** --include-path=benchmarks/** --include-path=build-common/** --include-path=test-instrumenter/** --exclude-path=compiler/android-tests/** --exclude-path=repo/artifacts-tests/** --exclude-path=repo/codebase-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-compiler-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-native-compiler-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-forward-compatibility-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-tests/** --exclude-path=compiler/daemon/daemon-tests/** --exclude-path=compiler/fir/analysis-tests/** --exclude-path=compiler/fir/modularized-tests/**"
          sarif_file: findings.sarif
      - uses: github/codeql-action/upload-sarif@v4
        if: always()
        continue-on-error: true
        with:
          sarif_file: findings.sarif
          category: endorlabs-java-other
    env:
      # Auto-fallback safety net — if the local build fails on a runner, endorctl uses the platform's pre-computed reachability instead of failing the job. Full deep reachability is still the primary path on success.
      ENDOR_SCAN_ENABLE_PRECOMPUTED_CALLGRAPHS: "true"
  scan-kotlin-pr:
    name: "PR Scan: kotlin"
    needs: detect-changes
    if: github.event_name == 'pull_request' && needs.detect-changes.outputs.kotlin == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      id-token: write
      security-events: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
      - run: ./gradlew build -x test
      - uses: endorlabs/github-action@b8992820cc4d9c9e7ded5022adf6cabe2dc11946
        with:
          namespace: "endor-solutions-ypulse.github-ci.saber-e2e"
          scan_dependencies: true
          pr: true
          enable_pr_comments: true
          github_token: ${{ secrets.GITHUB_TOKEN }}
          enable_github_action_token: true
          additional_args: "--languages=kotlin --exclude-path=compiler/android-tests/** --exclude-path=repo/artifacts-tests/** --exclude-path=repo/codebase-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-compiler-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-native-compiler-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-forward-compatibility-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-tests/** --exclude-path=compiler/daemon/daemon-tests/** --exclude-path=compiler/fir/analysis-tests/** --exclude-path=compiler/fir/modularized-tests/** --quick-scan"
  scan-kotlin-monitor:
    name: "Monitor Scan: kotlin"
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      id-token: write
      security-events: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
      - run: ./gradlew build -x test
      - uses: endorlabs/github-action@b8992820cc4d9c9e7ded5022adf6cabe2dc11946
        with:
          namespace: "endor-solutions-ypulse.github-ci.saber-e2e"
          scan_dependencies: true
          pr: false
          enable_github_action_token: true
          additional_args: "--languages=kotlin --exclude-path=compiler/android-tests/** --exclude-path=repo/artifacts-tests/** --exclude-path=repo/codebase-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-compiler-tests/** --exclude-path=analysis/low-level-api-fir/low-level-api-fir-native-compiler-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-forward-compatibility-tests/** --exclude-path=compiler/build-tools/kotlin-build-tools-api-tests/** --exclude-path=compiler/daemon/daemon-tests/** --exclude-path=compiler/fir/analysis-tests/** --exclude-path=compiler/fir/modularized-tests/**"
          sarif_file: findings.sarif
      - uses: github/codeql-action/upload-sarif@v4
        if: always()
        continue-on-error: true
        with:
          sarif_file: findings.sarif
          category: endorlabs-kotlin
    env:
      # Auto-fallback safety net — if the local build fails on a runner, endorctl uses the platform's pre-computed reachability instead of failing the job. Full deep reachability is still the primary path on success.
      ENDOR_SCAN_ENABLE_PRECOMPUTED_CALLGRAPHS: "true"
  scan-javascript-typescript-libraries-pr:
    name: "PR Scan: javascript/typescript (libraries)"
    needs: detect-changes
    if: github.event_name == 'pull_request' && needs.detect-changes.outputs.javascript-typescript-libraries == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      id-token: write
      security-events: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: "20"
      - uses: endorlabs/github-action@b8992820cc4d9c9e7ded5022adf6cabe2dc11946
        with:
          namespace: "endor-solutions-ypulse.github-ci.saber-e2e"
          scan_dependencies: true
          pr: true
          enable_pr_comments: true
          github_token: ${{ secrets.GITHUB_TOKEN }}
          enable_github_action_token: true
          additional_args: "--languages=javascript,typescript --include-path=libraries/** --quick-scan"
  scan-javascript-typescript-libraries-monitor:
    name: "Monitor Scan: javascript/typescript (libraries)"
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      id-token: write
      security-events: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: "20"
      - uses: endorlabs/github-action@b8992820cc4d9c9e7ded5022adf6cabe2dc11946
        with:
          namespace: "endor-solutions-ypulse.github-ci.saber-e2e"
          scan_dependencies: true
          pr: false
          enable_github_action_token: true
          additional_args: "--languages=javascript,typescript --include-path=libraries/**"
          sarif_file: findings.sarif
      - uses: github/codeql-action/upload-sarif@v4
        if: always()
        continue-on-error: true
        with:
          sarif_file: findings.sarif
          category: endorlabs-javascript-typescript-libraries
    env:
      # Auto-fallback safety net — if the local build fails on a runner, endorctl uses the platform's pre-computed reachability instead of failing the job. Full deep reachability is still the primary path on success.
      ENDOR_SCAN_ENABLE_PRECOMPUTED_CALLGRAPHS: "true"
  scan-javascript-typescript-js-pr:
    name: "PR Scan: javascript/typescript (js)"
    needs: detect-changes
    if: github.event_name == 'pull_request' && needs.detect-changes.outputs.javascript-typescript-js == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      id-token: write
      security-events: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: "20"
      - uses: endorlabs/github-action@b8992820cc4d9c9e7ded5022adf6cabe2dc11946
        with:
          namespace: "endor-solutions-ypulse.github-ci.saber-e2e"
          scan_dependencies: true
          pr: true
          enable_pr_comments: true
          github_token: ${{ secrets.GITHUB_TOKEN }}
          enable_github_action_token: true
          additional_args: "--languages=javascript,typescript --include-path=js/** --quick-scan"
  scan-javascript-typescript-js-monitor:
    name: "Monitor Scan: javascript/typescript (js)"
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      id-token: write
      security-events: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: "20"
      - uses: endorlabs/github-action@b8992820cc4d9c9e7ded5022adf6cabe2dc11946
        with:
          namespace: "endor-solutions-ypulse.github-ci.saber-e2e"
          scan_dependencies: true
          pr: false
          enable_github_action_token: true
          additional_args: "--languages=javascript,typescript --include-path=js/**"
          sarif_file: findings.sarif
      - uses: github/codeql-action/upload-sarif@v4
        if: always()
        continue-on-error: true
        with:
          sarif_file: findings.sarif
          category: endorlabs-javascript-typescript-js
    env:
      # Auto-fallback safety net — if the local build fails on a runner, endorctl uses the platform's pre-computed reachability instead of failing the job. Full deep reachability is still the primary path on success.
      ENDOR_SCAN_ENABLE_PRECOMPUTED_CALLGRAPHS: "true"
  scan-javascript-typescript-wasm-pr:
    name: "PR Scan: javascript/typescript (wasm)"
    needs: detect-changes
    if: github.event_name == 'pull_request' && needs.detect-changes.outputs.javascript-typescript-wasm == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      id-token: write
      security-events: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: "20"
      - uses: endorlabs/github-action@b8992820cc4d9c9e7ded5022adf6cabe2dc11946
        with:
          namespace: "endor-solutions-ypulse.github-ci.saber-e2e"
          scan_dependencies: true
          pr: true
          enable_pr_comments: true
          github_token: ${{ secrets.GITHUB_TOKEN }}
          enable_github_action_token: true
          additional_args: "--languages=javascript,typescript --include-path=wasm/** --quick-scan"
  scan-javascript-typescript-wasm-monitor:
    name: "Monitor Scan: javascript/typescript (wasm)"
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      id-token: write
      security-events: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: "20"
      - uses: endorlabs/github-action@b8992820cc4d9c9e7ded5022adf6cabe2dc11946
        with:
          namespace: "endor-solutions-ypulse.github-ci.saber-e2e"
          scan_dependencies: true
          pr: false
          enable_github_action_token: true
          additional_args: "--languages=javascript,typescript --include-path=wasm/**"
          sarif_file: findings.sarif
      - uses: github/codeql-action/upload-sarif@v4
        if: always()
        continue-on-error: true
        with:
          sarif_file: findings.sarif
          category: endorlabs-javascript-typescript-wasm
    env:
      # Auto-fallback safety net — if the local build fails on a runner, endorctl uses the platform's pre-computed reachability instead of failing the job. Full deep reachability is still the primary path on success.
      ENDOR_SCAN_ENABLE_PRECOMPUTED_CALLGRAPHS: "true"
  scan-swift-pr:
    name: "PR Scan: swift"
    needs: detect-changes
    if: github.event_name == 'pull_request' && needs.detect-changes.outputs.swift == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      id-token: write
      security-events: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v6
      - uses: swift-actions/setup-swift@v2
      - uses: endorlabs/github-action@b8992820cc4d9c9e7ded5022adf6cabe2dc11946
        with:
          namespace: "endor-solutions-ypulse.github-ci.saber-e2e"
          scan_dependencies: true
          pr: true
          enable_pr_comments: true
          github_token: ${{ secrets.GITHUB_TOKEN }}
          enable_github_action_token: true
          additional_args: "--languages=swift --quick-scan"
  scan-swift-monitor:
    name: "Monitor Scan: swift"
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      id-token: write
      security-events: write
    steps:
      - uses: actions/checkout@v6
      - uses: swift-actions/setup-swift@v2
      - uses: endorlabs/github-action@b8992820cc4d9c9e7ded5022adf6cabe2dc11946
        with:
          namespace: "endor-solutions-ypulse.github-ci.saber-e2e"
          scan_dependencies: true
          pr: false
          enable_github_action_token: true
          additional_args: "--languages=swift"
          sarif_file: findings.sarif
      - uses: github/codeql-action/upload-sarif@v4
        if: always()
        continue-on-error: true
        with:
          sarif_file: findings.sarif
          category: endorlabs-swift
    env:
      # Auto-fallback safety net — if the local build fails on a runner, endorctl uses the platform's pre-computed reachability instead of failing the job. Full deep reachability is still the primary path on success.
      ENDOR_SCAN_ENABLE_PRECOMPUTED_CALLGRAPHS: "true"